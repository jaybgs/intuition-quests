-- Initial database setup script
-- Run this after creating the PostgreSQL database

-- Create database (run this manually or via psql)
-- CREATE DATABASE trustquests;

-- The Prisma migrations will handle all table creation
-- Just ensure the database exists and DATABASE_URL is set correctly

-- After running migrations, you can verify with:
-- SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';

